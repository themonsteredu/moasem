import type {ReactNode} from 'react'
export const metadata={title:'내 공부방 · 모아셈',robots:{index:false,follow:false},referrer:'no-referrer'}
export default function Layout({children}:{children:ReactNode}){return children}
