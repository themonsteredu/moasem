# 설치되는 부품 폴더 (npm install 로 언제든 다시 만들어짐)
/node_modules

# 빌드 결과물
/.next
/out
/build
next-env.d.ts

# 환경변수 (Supabase 키·관리자 키 등 비밀 정보)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 배포 도구
.vercel

# 로그·디버그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo

# 운영체제가 만드는 잡파일
.DS_Store
Thumbs.db

# Supabase CLI local cache
/supabase/.temp/
